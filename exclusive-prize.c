#include <stdio.h>

int main() {
    int len;
    scanf("%d", &len);
    int nums[len];
    for (int i = 0; i < len; i++) {
        scanf("%d", &nums[i]);
    }
    int packets = 0;
    for (int i = 0; i < len; i++) {
        // implement later
    }
    return 0;
}